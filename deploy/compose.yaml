services:
  chrono:
    image: ${CHRONO_IMAGE:-ghcr.io/casys-ai/mcp-chrono@sha256:b9332fdf44634a565596d5cee6e64c9735b35d22299fab806631eaf86aa479a6}
    environment:
      MCP_BEARER_TOKEN: ${MCP_BEARER_TOKEN:?Set MCP_BEARER_TOKEN in deploy/.env}
    ports:
      - "127.0.0.1:3025:3025"
    volumes:
      - chrono-data:/data
    restart: unless-stopped
    privileged: false
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true

volumes:
  chrono-data:
